/*39. Na matematica, um quadrado perfeito é um numero inteiro nao negativo que pode ser expresso
como o quadrado de outro numero inteiro, ou seja, um numero cujo valor da raiz quadrado é inteiro.
Com base nessa definição, faça um algoritmo que leia um numero e informe se o mesmo é ou nao é
um quadrado perfeito.
Exemplos de números quadrados perfeitos: 
0 = 0², 1 = 1², 4 = 2², 9 = 3², 16 = 4²

*/

#include<stdio.h>
#include<locale.h>
#include<math.h>

int main(){
setlocale(LC_ALL, "portuguese");
	int num1;
	
	
	system("pause");
	return 0;
}